# Public origin of the FastAPI backend (no trailing slash).
# Leave empty in Vite dev to use same-origin `/api/v1` (see `vite.config.ts` proxy).
VITE_PUBLIC_WEB_BASE_URL=http://127.0.0.1:8000

# Optional: surfaced for future client retry UX (currently server-side).
VITE_APARU_HTTP_RETRIES=3
